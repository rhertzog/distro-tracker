- Add cron entries to update the PTS
- Add apache/nginx configuration files ready to use with the default
  gunicorn setup
- Move configuration settings to /etc
- Automatically collect static files in postinst
- Replace embedded jquery files with packaged files
